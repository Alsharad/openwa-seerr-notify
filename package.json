{
  "name": "openwa-seerr-notify",
  "version": "1.11.1",
  "private": true,
  "type": "module",
  "description": "OpenWA plugin: deliver Overseerr/Jellyseerr notifications over WhatsApp, routed per recipient.",
  "license": "MIT",
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "node build.mjs",
    "test": "node --import tsx --test *.test.ts",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm test && npm run build"
  },
  "devDependencies": {
    "@types/node": "^22.20.0",
    "esbuild": "^0.24.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
